[package]
name = "tacticaltray-linux"
version = "0.1.0"
edition = "2024"

[dependencies]
ksni = { version = "0.3", features = ["blocking"] }
image = { version = "0.25", features = ["png"] }
sysinfo = "0.33"
glib = "0.18"

[features]
default = []